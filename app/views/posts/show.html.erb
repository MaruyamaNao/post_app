  <%= @post.text %>
  <%= @post.created_at %>

<% if user_signed_in? %>
  <%= link_to '編集', "/posts/#{@post.id}/edit", method: :get %>
  <%= link_to '削除', "/posts/#{@post.id}", method: :delete %>
<% else %>
<% end %>